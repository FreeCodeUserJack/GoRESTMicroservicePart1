package main

import (
	"testing"
	// "github.com/stretchr/testify/assert"
)

func TestAdd(t *testing.T) {
	t.Run("add 2 numbers correctly", func(t *testing.T) {
		// a := 1
		// b := 2

		// want := 3
		// got := Add(a, b)

		// assert.Equal(t, got, want, "these two should be equal")
		// if got != want {
		// 	t.Errorf("got %d, want %d", got, want)
		// }
	})
}